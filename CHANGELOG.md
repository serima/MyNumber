# Changelog

## 2.0.0 (2026-07-05)

### Fixed

- **法人番号の検証ロジックを省令通りに修正しました。**
  法人番号の検査用数字は12桁の基礎番号の「前」に付される先頭桁ですが(番号利用法施行令 第35条第1項)、
  従来の実装は「末尾桁」を検査用数字として扱っていたため、実在する正しい法人番号
  (例: 国税庁 `7000012050002`)を不正と判定していました。
  `verifyCompany()` は先頭桁を検査用数字として検証するようになります。
  **旧実装で「正当」と判定されていた値は本修正後は不正と判定される場合があります(その逆も同様)。**

### Changed

- PHP の最低要求バージョンを 8.2 に引き上げ(`declare(strict_types=1)`、引数・戻り値の型宣言を追加)
- PHPUnit 4 → PHPUnit 11 に更新(テストはデータプロバイダ + Attribute 形式に書き換え)
- Travis CI → GitHub Actions に移行(PHP 8.2 / 8.3 / 8.4 / 8.5 でテスト)
- `phpunit.xml` → `phpunit.xml.dist`(モダンなスキーマに更新)
- `composer.json` から packagist.jp のカスタムリポジトリ設定を削除
- リンク切れとなっていた旧 e-Gov(law.e-gov.go.jp)へのリンクを laws.e-gov.go.jp に更新

## 0.0.2 / 0.0.1 (2015-10-09)

- 初期リリース(個人番号・法人番号の検証)
